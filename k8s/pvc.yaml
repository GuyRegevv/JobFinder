apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jobfinder-data
  namespace: jobfinder
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # k3s uses local-path provisioner by default
  storageClassName: local-path
