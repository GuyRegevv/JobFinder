apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: jobfinder

resources:
  - namespace.yaml
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  # cloudflare-tunnel.yaml is deployed separately (different namespace)
